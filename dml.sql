-- orden para conectar el usuario root a msqls: 
sudo mysql -u root -p
 -- desonectar de la base:
 quit
-- orden para que nos enseñe las bases:
show databases;
 -- entrar en la base:
 use "insertas el nombre de la base que quieres";
-- abrir version:
select version();
-- abrir fecha y hora:
select now();
 -- mostrar usuario que esta usando la base de datos:
 select user();
 